# How to import data in FalkorDB from Neo4j

Use the repository (https://github.com/FalkorDB/Neo4j-to-FalkorDB)[Neo4j-to-FalkorDB] to export CSV data from Neo4j.

Then put the csv data in a folder in this repository named `csv_output`.

To start the import process run the script `run_bulk_import.sh`